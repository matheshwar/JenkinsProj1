pipeline {
    agent any
    stages {
        stage('Install Dependencis') {
            steps {
                script {
                    // Ensure npm is installed
                    sh 'npm install'
                }
            }
        }
    }
    
}
